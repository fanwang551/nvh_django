相关表“class ConditionMeasurePoint(models.Model):
    """工况测点维度表：仅存储工况与测点"""

    work_condition = models.CharField(max_length=100, verbose_name='工况', db_index=True)
    measure_point = models.CharField(max_length=100, verbose_name='测点', db_index=True)

    class Meta:
        db_table = 'condition_measure_point'
        verbose_name = '工况测点'
        verbose_name_plural = '工况测点'
        indexes = [
            models.Index(fields=['work_condition', 'measure_point'], name='idx_cm_wc_mp'),
        ]
        ordering = ['work_condition', 'measure_point']

    def __str__(self) -> str:
        return f"{self.work_condition} - {self.measure_point}"


class AcousticTestData(models.Model):
    """声学测试数据表（频谱、总声压级等）"""

    vehicle_model = models.ForeignKey(
        'modal.VehicleModel',
        on_delete=models.CASCADE,
        related_name='acoustic_test_data',
        verbose_name='车型信息'
    )

    condition_point = models.ForeignKey(
        'acoustic_analysis.ConditionMeasurePoint',
        on_delete=models.PROTECT,
        related_name='test_data',
        verbose_name='工况测点'
    )

    # 频谱数据（约12000个数据点）
    # 格式：{"frequency": [...], "dB": [...]}
    spectrum_json = models.JSONField(null=True, blank=True, verbose_name='频谱数据JSON')

    # 总声压级数据（约20个数据点）
    # 格式：{"time": [...], "OA": [...]}
    oa_json = models.JSONField(null=True, blank=True, verbose_name='总声压级JSON')

    # 语音清晰度与有效值
    speech_clarity = models.DecimalField(max_digits=10, decimal_places=4, null=True, blank=True, verbose_name='语音清晰度')
    rms_value = models.DecimalField(max_digits=10, decimal_places=4, null=True, blank=True, verbose_name='有效值')

    test_date = models.DateField(null=True, blank=True, verbose_name='测试日期')”
当前：用户选择车型-工况-测点后输出对应测点的频谱曲线和总声压级曲线
0、通过测点判断具体的单位,可以在condition_measure_point 表中添加一列用于判断单位；condition_measure_point表中数据分为三种类型，分别是噪声、振动和转速；噪声数据存储spectrum_json、oa_json、speech_clarity、rms_value；
振动数据存储spectrum_json、振动_json、rms_value；转速数据存储spectrum_json、rss_value；我应该重新设计表吗，还是表保持不变，存储的这些参数都是用于绘图；但是由于不同测点的测试数据类型不同需要绘制不同的表，
例如：选择了10个工况测点，横轴表示工况，纵轴表示具体的数值（speech_clarity或者rms_value或者rpm）、同种数据类型在同一表中显示，不同在不同表中显示，他们可以通过工况测点区分
例如选择了“压缩机双开1档
压缩机双开2档
压缩机双开3档
压缩机双开4档
压缩机双开5档
压缩机双开6档
压缩机双开7档
压缩机双开8档
压缩机双开9档
”九个工况和6个测点“压缩机本体Y		压缩机本体Z		车前0.5米			车前1.65米			压缩机转速	电子扇转速
m/s2		m/s2		dB(A)			dB(A)			rpm	rpm
振动曲线	RMS值	振动曲线	RMS值	噪声曲线	RMS值	语音清晰度%	噪声曲线	RMS值	语音清晰度%		
”，横轴都是九个工况，分为四个图表展示，图表1纵轴为噪声的RMS 包括车前0.5m和车前1.65m两条曲线，图表2纵轴为噪声的语音清晰度包括车前0.5m和车前1.65m两条曲线，图表3为振动的RMS包括压缩机本体Y和压缩机本体Z两条曲线； 图表4为转速包括压缩机转速和电子扇转速两条曲线；可以单独加一个rpm字段

1、需求变更：数据分为频谱和振动他们单位不同无法再同一图像中绘制曲线，
如果出现dB就是频谱则绘制频谱曲线和总声压级曲线，横轴为频率纵轴为dB；出现m/s2就是振动横轴为频率纵轴为m/s2；总声压级曲线纵轴也为m/s2,。通过测点判断具体的单位,可以在condition_measure_point
表中添加一列用于判断单位。
2、需求添加
输入：车型-工况-测点
输出：oa曲线；振动曲线；清晰度曲线；转速曲线；转速曲线
数值由数据库决定，包括三个参数： 曲线 有效值 转速 

佬们，目前比较成熟的NL2SQL项目是什么
我的需求：输入自然语言查找数据库给出结果并可视化，例如对比Tesla model y和model 3 2024年的每月销量→查询数据库后画出折线图
目前找到的:WrenAI vanna sqlbot pandas ai;有佬试用过吗，那个方案比较好有没有比较成熟的方案